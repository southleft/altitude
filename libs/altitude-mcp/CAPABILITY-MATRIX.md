# Altitude MCP — capability matrix

GENERATED by `libs/altitude-mcp/scripts/build-capability-matrix.mjs` from a REAL MCP handshake against
`libs/altitude-mcp/src/server.mjs` (the name and required-argument columns come off the wire,
the same way `test/smoke.mjs` talks to the server) — this file cannot list a tool, resource, or
prompt the server does not actually register, and cannot omit one it does. `--check` gates it in
CI via `scripts/check-mcp-docs.mjs`. Regenerate: `node libs/altitude-mcp/scripts/build-capability-matrix.mjs`.

Format follows Carbon's tool-docs convention: intent -> surface -> required filters -> expected
result fields -> common failure mode. "Common failure mode" is real — every entry cites an actual
`code` raised somewhere in `server.mjs` or `src/lib/*.mjs`; where none exists, it says so.

## Tools (9)

| Intent | Tool | Required filters | Expected result fields | Common failure mode |
|---|---|---|---|---|
| Discover components, or decide between two, from each one's purpose / whenToUse / whenNotToUse | `altitude_list_components` | none | count, guidanceCoverage, components[].{tag, className, summary, migration, guidance}; `guidance` is null for a component nobody has written guidance for, and guidanceCoverage says how many have it | none observed — an unmatched `filter` returns an empty components[], not an error. With no docs build in the checkout every `guidance` is null and guidanceCoverage.reason names `pnpm --filter al-app-docs build` (docs-artifacts.mjs) |
| Get everything needed to write correct code for one component — a working example first, then the API | `altitude_get_component` | tag | tag, className, examples[].{title, code, react, source}, examplesNote, guidance, guidanceNote, react.{component, importPath, eventProps}, a11y.{semantics, measured, obligations}, attributes, slots, events, cssParts, cssProperties, migration, schema, story, contract?, referenceDoc? | unknown `tag` -> {error, code: ERR_UNKNOWN_COMPONENT} (tools.mjs). Every optional block degrades to an explicit note rather than an absent key: no docs build -> examplesNote/guidanceNote name `pnpm --filter al-app-docs build`; no axe sweep -> a11y.measured.measured === false with a reason (never a pass); no wrapper -> react null + reactNote |
| Check <al-*> / @southleft/al-react usage against the shipped component contracts before it ships | `altitude_validate` | none | apiVersion, type, data.violations[].{component, rule, code, fix} | violations carry ERR_UNKNOWN_COMPONENT / ERR_UNKNOWN_ATTRIBUTE / ERR_INVALID_ENUM / ERR_TYPE_MISMATCH (cli/validate.mjs, wrapped verbatim); the CLI itself missing -> ERR_MISSING_ARTIFACT (paths.mjs) |
| Find the right token, and the CSS properties it is legal in | `altitude_get_tokens` | none | count, totalMatched, tokens[].{name, tier?, brand?, mode?, rawValue?, resolvedValue?, type, cssType, cssProperties, dtcgType, description}; `cssType` is the AUTHORED surface (spacing, borderRadius, …) and is not derivable from the coarse `dtcgType` | `brand` outside the shipped enum (altitude\|southleft — a pruned brand) is rejected at the protocol level by the zod input schema, before the handler runs; a missing dist/css/tokens.json (and its styles/dist/ fallback) -> ERR_MISSING_ARTIFACT (paths.mjs, HINTS.tokens) |
| Turn a described intent ("a slightly stronger neutral surface", "the danger fill on hover") into ONE token, instead of guessing between weak / default / strong / bold | `altitude_resolve_token` | none | token, cssVar, values{light,dark}, reason, description, ladder.{key,rungs,chosen,index,of,collapsed}, nearMisses[].{token,values,why}, warnings[], propertyCheck, intent, source. With `report: true`: totals.{collapsedLadders,collidingStepGroups} and byScope per brand+mode. The emphasis ladder is NOT monotonic (weak === strong for background.neutral in light), so a collapsed rung is reported in `reason`/`warnings` rather than silently chosen | role/surface omitted -> ERR_INCOMPLETE_TOKEN_INTENT; a role or surface the token set does not ship -> ERR_UNKNOWN_TOKEN_ROLE / ERR_UNKNOWN_TOKEN_SURFACE, each naming the values that DO exist (token-resolve.mjs). No AI-readiness token digest -> ERR_MISSING_ARTIFACT naming `node scripts/ai-readiness/build-tokens-digest.mjs`; a digest built with no styles/dist-v5 output -> the same code naming `build:tokens`, since that is the artifact actually missing |
| Find a Phosphor icon by name/tag/category and get its exact import + registerIcons() snippet | `altitude_search_icons` | none | count, icons[].{name, pascalName, categories, tags, exportName, snippet} | none observed — an unmatched query/category returns an empty icons[], not an error |
| Derive a WCAG-AA token override set from a short prompt or an explicit art-direction object | `altitude_generate_theme` | none | name, quip, mode, personality, palette, receipts, direction, source | both the built theme-engine barrel and its TypeScript source missing (incomplete checkout) -> a thrown Error surfaced by toolHandler() as {error, code: ERR_TOOL_FAILURE} (theme.mjs has no dedicated code for this case) |
| Figma <-> code parity for one component or the full report, for any registered design-system project | `altitude_check_parity` | none | project, manifestPresent, components[].{tag, status, driftBasis, aiPrompt, ...}, scope, observation | unknown `project` -> {error, code: ERR_UNKNOWN_DS_PROJECT, knownProjects} (ds-project.mjs); a missing or malformed .altitude/ds-projects.json -> ERR_MISSING_DS_REGISTRY / ERR_INVALID_DS_REGISTRY; unknown `tag` -> {error, code: ERR_UNKNOWN_COMPONENT} |
| Discover design-system project ids and their Figma file / brand / docs / parity-manifest coordinates | `altitude_list_ds_projects` | none | default, projects[].{id, name, brand, isDefault, figma, storybook, docs, parityManifest} | missing or malformed .altitude/ds-projects.json -> {error, code: ERR_MISSING_DS_REGISTRY \| ERR_INVALID_DS_REGISTRY} |

## Resources (6 fixed + 1 templated)

| Intent | Resource | Required filters | Expected result fields | Common failure mode |
|---|---|---|---|---|
| Pull the whole Custom Elements Manifest for local analysis or caching | `altitude://components` | none | the raw CEM JSON (modules[].declarations[]…) — same file altitude_list_components/altitude_get_component parse | missing custom-elements.json -> JSON content {error, code: ERR_MISSING_ARTIFACT, path, hint}, never a thrown protocol error |
| Pull the whole resolved --al-* token set | `altitude://tokens` | none | the raw dist/css/tokens.json contents (flat name -> value map) | missing dist/css/tokens.json and its styles/dist/ fallback -> {error, code: ERR_MISSING_ARTIFACT, hint: HINTS.tokens} |
| Pull the full axe-core accessibility sweep across every component | `altitude://a11y-report` | none | the raw .altitude/a11y/report.json contents | missing report.json -> {error, code: ERR_MISSING_ARTIFACT, hint: "pnpm run a11y:report"} |
| Pull the fleet-probe-shaped CEM digest (attributes/slots/events + doNotFlag carve-outs) | `altitude://ai-readiness/cem-digest` | none | the raw .altitude/ai-readiness/cem-digest.json contents | missing digest -> {error, code: ERR_MISSING_ARTIFACT, hint: "node scripts/ai-readiness/build-cem-digest.mjs"} |
| Pull the fleet-probe-shaped tokens digest (families + do-not-invent conventions) | `altitude://ai-readiness/tokens-digest` | none | the raw .altitude/ai-readiness/tokens-digest.json contents | missing digest -> {error, code: ERR_MISSING_ARTIFACT, hint: "node scripts/ai-readiness/build-tokens-digest.mjs"} |
| Pull the raw design-system project registry | `altitude://ds-projects` | none | the raw .altitude/ds-projects.json contents (default + projects{}) | missing registry -> {error, code: ERR_MISSING_ARTIFACT} (a tracked file; restore via git) |
| Pull one design system's Figma <-> code parity manifest | `altitude://parity-manifest/{project}` | {project} — any id from altitude_list_ds_projects / altitude://ds-projects | the raw parity-manifest.json contents for that project (components{}, figmaOnly[], figmaLastRefreshed) | unknown project id -> {error, code: ERR_UNKNOWN_DS_PROJECT, knownProjects}; missing/malformed registry -> ERR_MISSING_DS_REGISTRY / ERR_INVALID_DS_REGISTRY — same codes altitude_check_parity surfaces, always as resource content, never a thrown protocol error (verified: server still starts and lists resources with ds-projects.json moved aside) |

## Prompts (4)

| Intent | Prompt | Required arguments | Expected result fields | Common failure mode |
|---|---|---|---|---|
| Draft a Figma <-> code reconciliation plan for one component, grounded in the live parity engine | `audit_component_parity` | tag | one user message: current status, drift basis, the parity engine's ready-to-run aiPrompt (if drifted), a pointer to the altitude-figma-sync skill | unknown `tag` -> message body {error, code: ERR_UNKNOWN_COMPONENT}; unknown `project` -> {error, code: ERR_UNKNOWN_DS_PROJECT, knownProjects} |
| Draft an altitude_generate_theme call (prompt + optional mode/personality) and how to apply the result | `generate_brand_theme` | prompt | one user message naming the exact tool call to make, plus how to apply the returned palette | `prompt` missing or over 80 chars is rejected at the protocol level by the zod argsSchema, before the callback runs |
| Draft an altitude_validate call for a proposed snippet, plus the code -> fix self-heal loop | `check_snippet_convention` | markup | one user message naming the exact tool call and the REPAIR.md-keyed self-heal loop | empty `markup` is rejected at the protocol level by the zod argsSchema (min length 1), before the callback runs |
| Pre-flight a proposed component name against the layout-primitive gate, then walk the authoring checklist | `scaffold_component` | name | one user message: either a structured ERR_LAYOUT_SUSPECT/ERR_INVALID_COMPONENT_NAME refusal, or the plop + authoring-skill steps | a name matching the plop LAYOUT_SUSPECT gate -> {error, code: ERR_LAYOUT_SUSPECT}; a non-dash-case name -> {error, code: ERR_INVALID_COMPONENT_NAME} |
