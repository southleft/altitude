{
  "name": "al-story-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Renders every component story with real Lit, so the accessibility sweep does not need a Storybook build. Drop-in for the index.json + iframe.html?id= + #storybook-root contract that scripts/build-a11y-report.mjs consumes.",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --port 6019"
  }
}
