{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://southleft.com/altitude/al-chip.schema.json",
  "title": "ALChip (al-chip)",
  "description": "Component: al-chip",
  "type": "object",
  "altitude": {
    "tagName": "al-chip",
    "className": "ALChip",
    "module": "components/chip/chip.ts",
    "migration": {
      "state": "scoped-complete",
      "react19": true,
      "headless": false,
      "ssr": false
    },
    "slots": [
      {
        "description": "The chip content",
        "name": ""
      }
    ],
    "events": [
      {
        "name": "onChipClose",
        "description": "Fired when the chip's remove control is activated. Carries no detail payload; read the chip's own properties from `event.target`."
      }
    ],
    "cssParts": [],
    "cssProperties": [
      {
        "description": "Chip surface. Defaults to `--al-theme-color-background-default-strong`.",
        "name": "--al-chip-background"
      },
      {
        "description": "Label colour. Defaults to `--al-theme-color-content-default-weak`.",
        "name": "--al-chip-color"
      },
      {
        "description": "Border shorthand. Defaults to `none`.",
        "name": "--al-chip-border"
      },
      {
        "description": "Corner radius. Defaults to `4px`.",
        "name": "--al-chip-border-radius"
      },
      {
        "description": "Padding shorthand. Defaults to `space-xxs space-sm`.",
        "name": "--al-chip-padding"
      },
      {
        "description": "Gap between label and icon. Defaults to `space-xs`.",
        "name": "--al-chip-gap"
      },
      {
        "description": "Label family. Defaults to `inherit`.",
        "name": "--al-chip-font-family"
      },
      {
        "description": "Defaults to `pointer`; set `default` for a non-interactive chip.",
        "name": "--al-chip-cursor"
      }
    ]
  },
  "properties": {
    "tagName": {
      "const": "al-chip"
    },
    "attributes": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "variant": {
          "description": "Variant\n- **default** A chip with a strong contrast background\n- **secondary** A chip with a outline\n- **info** A chip with a info background\n- **success** A chip with a success background\n- **warning** A chip with a warning background\n- **danger** A chip with a danger background",
          "type": "string",
          "enum": [
            "secondary",
            "info",
            "success",
            "warning",
            "danger"
          ]
        },
        "type": {
          "description": "typescript: 'squared'",
          "type": "string"
        },
        "isDismissible": {
          "description": "Dismissible property\n- **true** Adds an X to hide the chip on click\n- **false** Hides the X so the chip can not be hidden",
          "type": "boolean"
        },
        "isDismissed": {
          "description": "Is dismissed property\n- **true** Hides the chip\n- **false** Shows the chip",
          "type": "boolean"
        }
      }
    },
    "children": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  },
  "required": [
    "tagName"
  ]
}
