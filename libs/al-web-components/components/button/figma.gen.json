{
  "$comment": "Per-component Figma-generation config (scripts/contracts/figma/component-config.mjs — see it for every recognized key and its live-confirmed reasoning). OPTIONAL: a component without this file generates with the documented defaults; al-button ships one as the worked exemplar because these values are ITS confirmed judgment calls from the T12–T32 pilot (they happen to equal the library defaults today — this file is where they change if they ever should). Facts (props/slots/states/tokens) stay in .altitude/contracts/<project>/al-button.contract.json; only generation judgment calls live here.",
  "iconSizeVar": "theme/icon/md",
  "fullWidthExtraPx": 160,
  "label": { "fontStyle": "Bold", "fontSize": 14 }
}
