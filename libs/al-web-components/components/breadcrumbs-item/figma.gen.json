{
  "$comment": "Per-component Figma-generation config (scripts/contracts/figma/component-config.mjs). textContent: the separator div renders a literal '/' — breadcrumbs-item.ts line ~117: `${this.hasSeparator === true ? html` <div class=\"al-c-breadcrumbs__separator\" aria-hidden=\"true\">${'/'}</div> ` : html``}` — curated here until the measurement pass captures leaf text generically (walkthrough, 2026-08-26).",
  "textContent": { "al-c-breadcrumbs__separator": "/" }
}
