{
  "$comment": "Per-component Figma-generation config (see altitude-figma-generate skill). Molecules batch, 2026-08-26. anatomyCase: label shown. nestedIconGlyphs ORDERED: template renders minus button then plus (input-stepper.ts imports ALIconMinus/ALIconAdd) — Phosphor 'minus'/'plus', consumed in document order. sheet.cellWidth: this component's natural width exceeds the library's default 200px sheet pitch (sheet-style.mjs SHEET_CELL_WIDTH_PX), so its instances bled across the border-collapse grid lines and clipped their own helper text. Measured live 2026-08-27 from the widest instance in its own prop sheet, rounded up with breathing room. This is the documented per-component override the generator expects - do NOT re-measure instances inside the build (the sheet-style.mjs header explains why: ~30s per-call ceiling). | v2: anatomyCase re-pinned for the same reason — the new Variant dimension changed the case ids and the auto-pick took `Label=hidden,Variant=segmented`. Sample the default shape: label shown, segmented.",
  "anatomyCase": "Label=shown,Variant=segmented",
  "nestedIconGlyphs": {
    "al-icon": [
      "minus",
      "plus"
    ]
  }
}
