{
  "$comment": "Per-component Figma-generation config (scripts/contracts/figma/component-config.mjs). anatomyCase: sample the LABEL form for the contract's anatomy — alphabetical-first picked 'Variant=danger,Shape=dot', which made every generated Badge render as a dot (walkthrough finding, 2026-08-26). The dot form (code prop `isDot`, the real set's Type=Dot) is a future second-case expression, not the default sample.",
  "anatomyCase": "Variant=default,Shape=label"
}
