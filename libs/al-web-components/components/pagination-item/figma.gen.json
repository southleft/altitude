{
  "$comment": "Per-component Figma-generation config (see altitude-figma-generate skill). anatomyCase: the default alphabetical-first rule sampled 'Selected=No,Kind=expandable', and that case is not a poorer variant of this component — it is a different element. render() emits a bare <slot> when isExpandable is true (pagination-item.ts:88-89), so the sampled tree had NO <a class=\"al-c-pagination__link\"> at all: 2 nodes and 4 tokens, against 3 nodes and 26 for Kind=number. Everything the component is styled with — its background, content colour, radius, padding, the selected and disabled treatments in pagination-item.scss — hangs off that link, so the contract's headline anatomy and `tokens` described almost nothing, and the canvas's correct bindings read as 'missing-in-code' in the diff. Same class as the al-badge dot case recorded in emit-contracts.mjs buildAnatomy(). Kind=number is the ordinary page number, which is what this component is.",
  "anatomyCase": "Selected=No,Kind=number"
}
