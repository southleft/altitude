import { createComponent } from '@lit-labs/react';
import { SLIcon{{pascalCase name}} as SLWebIcon{{pascalCase name}} } from 'sl-web-components/dist/sl/components/icon/icons/{{dashCase name}}';
import register from 'sl-web-components/dist/sl/directives/register';
import React from 'react';
import PackageJson from '../../../../package.json';

const elementMap = register({
  elements: [SLWebIcon{{pascalCase name}}.el, SLWebIcon{{pascalCase name}}],
  suffix: PackageJson.version
});

export const SLIcon{{pascalCase name}} = createComponent({
  react: React,
  tagName: elementMap.get(SLWebIcon{{pascalCase name}}.el),
  elementClass: SLWebIcon{{pascalCase name}},
  events: {}
});
