import { html, unsafeCSS } from 'lit';
import { property } from 'lit/decorators.js';
import { SLElement } from '../SLElement';
import styles from './{{dashCase name}}.scss';

/**
 * Component: sl-{{dashCase name}}
 * 
 * - **slot**: The component content
 */
export class SL{{pascalCase name}} extends SLElement {
  static el = 'sl-{{dashCase name}}';

  static get styles() {
    return unsafeCSS(styles.toString());
  }

  render() {
    const componentClassNames = this.componentClassNames('sl-c-{{dashCase name}}', { });

    return html`
      <div class="${componentClassNames}">
        <slot></slot>
      </div>
    `;
  }
}

if ((globalThis as any).slAutoRegistry === true && customElements.get(SL{{pascalCase name}}.el) === undefined) {
  customElements.define(SL{{pascalCase name}}.el, SL{{pascalCase name}});
}

declare global {
  interface HTMLElementTagNameMap {
    'sl-{{dashCase name}}': SL{{pascalCase name}};
  }
}