import {html} from 'lit';
import { spread } from '../../../directives/spread';
import './{{dashCase name}}';

export default {
  title: 'Templates/{{titleCase name}}',
  component: 'sl-{{dashCase name}}',
  parameters: {
    status: { type: 'beta' },
    layout: 'fullscreen'
  },
  tags: [ 'autodocs' ]
};

const Template = (args) => html`<sl-{{dashCase name}} ${spread(args)} data-testid="{{dashCase name}}">Hello world</sl-{{dashCase name}}>`;

export const Default = Template.bind({});
Default.args = {};