import type { StoryObj } from '@storybook/react-webpack5';
import { AL{{pascalCase name}} } from '../..';
import React from 'react';

export default {
  title: 'Atoms/{{titleCase name}}',
  component: AL{{pascalCase name}},
  parameters: { status: { type: 'beta' } },
  args: { children: 'Hello world' },
};

export const Default: StoryObj<typeof AL{{pascalCase name}}> = { args: {} };
