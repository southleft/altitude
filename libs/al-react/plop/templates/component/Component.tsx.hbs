import React from 'react';
import { createComponent } from '@lit/react';
import { AL{{pascalCase name}} as ALWeb{{pascalCase name}} } from 'al-web-components/components/{{dashCase name}}';
import register from 'al-web-components/directives/register';
import PackageJson from '../../../package.json';

const elementMap = register({
  elements: [ALWeb{{pascalCase name}}.el, ALWeb{{pascalCase name}}],
  suffix: PackageJson.version
});

export const AL{{pascalCase name}} = createComponent({
  react: React,
  tagName: elementMap.get(ALWeb{{pascalCase name}}.el),
  elementClass: ALWeb{{pascalCase name}},
  events: {}
});
