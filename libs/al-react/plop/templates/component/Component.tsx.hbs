'use client';

import React from 'react';
import { createComponent } from '@lit/react';
import { AL{{pascalCase name}} as ALWeb{{pascalCase name}} } from '@southleft/al-web-components/components/{{dashCase name}}';
import register from '@southleft/al-web-components/directives/register';
import PackageJson from '../../../package.json';

const elementMap = register({
  elements: [ALWeb{{pascalCase name}}.el, ALWeb{{pascalCase name}}],
  suffix: PackageJson.version
});

export const AL{{pascalCase name}} = createComponent({
  react: React,
  tagName: elementMap.get(ALWeb{{pascalCase name}}.el),
  elementClass: ALWeb{{pascalCase name}},
  // Map every custom event the web component dispatches so it surfaces as a
  // React prop, e.g. `onChange: 'al-{{dashCase name}}-change'`. An empty map
  // silently drops events — see spec 2026-08-18-unified-jsdoc-and-generated-ai-docs R5.
  events: {}
});
