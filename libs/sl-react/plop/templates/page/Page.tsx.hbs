import * as React from 'react';
import classnames from 'classnames';
import '!style-loader!css-loader!sass-loader!./{{pascalCase name}}.scss';

/**
 * Page: SL{{dashCase name}}
 */
export interface SL{{pascalCase name}}Props extends React.HTMLAttributes<HTMLDivElement>{
  /**
   * CSS class names that can be appended to the component.
   */
  styleModifier?: string;
}

export const SL{{pascalCase name}} = ({
  styleModifier,
  ...other
}: SL{{pascalCase name}}Props) => {
  const componentClassNames = classnames(
    'sl-l-{{dashCase name}}',
    styleModifier,
    {}
  );

  return (
    <div className={componentClassNames} {...other}>
      {children}
    </div>
  );
};
