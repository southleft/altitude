{
  "name": "@southleft/altitude",
  "description": "Design system created by Southleft.com",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "react": "19.2.7",
      "react-dom": "19.2.7"
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "@swc/core"
    ]
  },
  "scripts": {
    "build": "pnpm --filter @southleft/al-web-components build && pnpm --filter @southleft/sl-web-components build && pnpm --filter @southleft/al-react build",
    "build:app-angular": "pnpm --filter al-app-angular build --output-path ../../dist/angular --base-href /angular/browser/",
    "build:app-react": "pnpm --filter al-app-react build --base=/react --outDir ../../dist/react",
    "build:app-svelte": "pnpm --filter al-app-svelte build --base=/svelte --outDir ../../dist/svelte",
    "build:app-web-components": "pnpm --filter al-app-web-components build",
    "build:app-mfe": "pnpm --filter al-app-mfe build",
    "build:app-ssr": "pnpm --filter al-app-ssr build",
    "build:app-astro": "pnpm --filter al-app-astro build",
    "build:app-southleft": "pnpm --filter al-app-southleft build",
    "build:app-docs": "pnpm --filter al-app-docs build",
    "build:app-home": "pnpm --filter al-app-home build",
    "build:fixtures": "pnpm run build:app-web-components && pnpm run build:app-react && pnpm run build:app-mfe && pnpm run build:app-ssr && pnpm run build:story-fixture",
    "build:all": "pnpm run build && pnpm run build:app-angular && pnpm run build:app-react && pnpm run build:app-svelte && pnpm run build:app-astro && pnpm run build:app-southleft && pnpm run build:app-docs && pnpm run copy:components && pnpm run build:app-home",
    "copy:components": "cp -r ./libs/al-web-components/dist ./dist/al-web-components",
    "dev:index": "npx serve ./dist",
    "parity:seed": "node scripts/figma-parity/seed-manifest.mjs",
    "parity:synced": "node scripts/figma-parity/mark-synced.mjs",
    "parity:refresh": "node scripts/figma-parity/refresh-figma-digests.mjs",
    "parity:projects": "node scripts/figma-parity/list-projects.mjs",
    "parity:seed:sl": "node scripts/figma-parity/seed-manifest.mjs --project southleft",
    "parity:synced:sl": "node scripts/figma-parity/mark-synced.mjs --project southleft",
    "parity:refresh:sl": "node scripts/figma-parity/refresh-figma-digests.mjs --project southleft",
    "//contracts:emit": "Emits per-component contracts (props/events/slots/anatomy/token bindings/a11y/figma+code bindings) for every parity-tracked component of the active project into .altitude/contracts/<project>/. See .altitude/contracts/README.md. --check ajv-validates output against contract.schema.json.",
    "contracts:emit": "node scripts/contracts/emit-contracts.mjs",
    "contracts:emit:sl": "node scripts/contracts/emit-contracts.mjs --project southleft",
    "//contracts:canvas": "Extracts CANVAS contracts (variant axes, component properties, bound-variable names, text styles, states, shallow anatomy) from live Figma component sets over scripts/figma-atoms/mcp-shim.mjs into .altitude/figma-sync/<project>/canvas-contracts/. See .altitude/contracts/canvas-contract.schema.json. --component <tag> extracts one set; --from-fixture runs offline against scripts/contracts/__fixtures__/canvas-sample.json.",
    "contracts:canvas": "node scripts/contracts/extract-canvas.mjs",
    "contracts:canvas:sl": "node scripts/contracts/extract-canvas.mjs --project southleft",
    "//contracts:diff": "Property-level CODE <-> CANVAS contract diff (T6, contract-diff.mjs) for one tag or --all: which props/variant values/states/token bindings disagree, degradation-aware. Missing canvas dump -> one clean line, exit 0. --json for machine output; --self-test runs the offline acceptance test against __fixtures__/diff-*-sample.*.json.",
    "contracts:diff": "node scripts/contracts/diff-contracts.mjs",
    "contracts:diff:sl": "node scripts/contracts/diff-contracts.mjs --project southleft",
    "//gate:docs": "Runs the SCOPE gate first: check:sl-scope re-derives each scoped design system's library.components allowlist from its own site's source, which is what makes 'adding a component to the site is what earns it a docs page' true. Then the docs coverage gate, which proves every component in the manifest has a generated page and every project's scope resolves.",
    "gate:docs": "pnpm run check:sl-scope && pnpm --filter al-app-docs check:coverage",
    "//gate:docs-generalises": "Runs a THROWAWAY third design-system project through the same functions the real sites are built from, and greps apps/docs/src for any project id, to prove a new client brand costs a registry entry and no code. Needs no build.",
    "gate:docs-generalises": "pnpm --filter al-app-docs check:third-project",
    "//gate:docs-panels": "Runs AFTER `pnpm --filter al-app-docs build` — it reads the built html in dist/docs to prove the status panels shipped no Figma file keys, node ids or internal script paths, and that every rendered status matches what the parity engine and the axe report say.",
    "gate:docs-panels": "pnpm --filter al-app-docs check:panels",
    "//gate:guidance": "Runs AFTER `pnpm --filter al-app-docs build`. The generated half of the docs has gates; this covers the AUTHORED half — the component guidance in apps/docs/src/content/guidance. It fails when guidance is written for something that is not a component, when a component page states neither guidance nor its absence, when a rendered panel is missing a required section (notably `when not to use`), when a cited file or its anchor text no longer exists, or when authored coverage drops below the floor.",
    "gate:guidance": "node scripts/check-guidance.mjs",
    "//story-fixture": "Renders every component story with real Lit — the accessibility sweep's fixture, replacing the storybook-static build. Same index.json + iframe.html?id= + #storybook-root contract. NOTE it is built through the al-web-components workspace package rather than `--dir`: story-fixture/ is nested inside that package, so `libs/*` does not match it, it has no node_modules of its own, and `pnpm --dir ... exec vite` finds no vite on a clean CI install.",
    "build:story-fixture": "pnpm --filter @southleft/al-web-components exec vite build -c story-fixture/vite.config.mjs",
    "a11y:report:fixture": "pnpm run build:story-fixture && node scripts/build-a11y-report.mjs --storybook libs/al-web-components/story-fixture/dist",
    "//a11y:report": "Regenerates .altitude/a11y/report.json, which the docs accessibility panels read. Prefer `a11y:report:fixture`, which builds the story fixture and points this at it. The --storybook flag keeps its name for compatibility; it takes any directory serving the index.json + iframe.html?id= contract. Never point it at a dev server (see the measurement note in scripts/build-a11y-report.mjs).",
    "a11y:report": "node scripts/build-a11y-report.mjs",
    "//a11y:report:docs": "Regenerates .altitude/a11y/report-docs.json - axe against the BUILT docs site, for components the story fixture does not cover (today: the Southleft brand layer, which has no stories of its own). Build the site first: pnpm --filter al-app-docs build. Measures each component's DEFAULT story only - the panel states that rather than implying full coverage.",
    "a11y:report:docs": "node scripts/build-a11y-docs-report.mjs",
    "gate:migration": "node scripts/check-migration-gate.js",
    "gate:baselines": "node scripts/check-baselines-gate.js",
    "gate:self-test": "bash scripts/__tests__/gate-self-test.sh",
    "check:ds-projects": "node scripts/check-ds-projects.mjs",
    "check:sl-scope": "node scripts/check-sl-scope.mjs",
    "check:exports": "node scripts/check-exports-map.js",
    "check:changesets": "node scripts/check-changesets-config.js",
    "check:jsdoc": "node scripts/check-jsdoc-dialect.js",
    "test:brands": "node scripts/check-brand-distinctiveness.js",
    "brands:compare": "node scripts/build-brand-compare.mjs",
    "test:scoped-theming": "node scripts/check-scoped-theming.mjs",
    "//test:contrast-axis": "Behavioural verification for <al-theme contrast> (spec 2026-08-22-token-debt-and-machine-readable-metadata). Proves contrast='more' raises --al-theme-opacity-disabled on a REAL rendered component and clears WCAG AA text contrast (4.5:1) against the worst realistic disabled pairing in both modes, and that nesting a bare or explicit contrast=\"normal\" <al-theme> inside one set to 'more' resets rather than inherits. Served through the same Vite-dev-server + static-harness infrastructure as test:scoped-theming (`.altitude/visual-compare/harness/contrast-axis.{html,js}`), not a live Storybook — Storybook was removed from this repo 2026-08-25. Requires `pnpm --filter @southleft/al-web-components build` first; first run compiles the whole component graph on demand and can take a minute.",
    "test:contrast-axis": "node scripts/verify-contrast-axis.mjs",
    "baselines:tokens": "node scripts/capture-token-baseline.js",
    "baselines:bundle": "node scripts/capture-bundle-baseline.js",
    "baselines:vrt": "playwright test --update-snapshots",
    "baselines:capture": "pnpm run build && pnpm --filter al-app-web-components build && pnpm run baselines:tokens && pnpm run baselines:bundle && pnpm run baselines:vrt",
    "test:vrt": "playwright test",
    "//test:unit": "Vitest 3 in BROWSER MODE (Playwright/chromium) over both component libraries. `wc` compiles @southleft/al-web-components from SOURCE; `react` resolves it through its exports map, i.e. dist/ - so run `pnpm run build` before `test:unit` or the React project tests a stale library. Ports 6180/6185. A min-count reporter fails the run if either project collects fewer tests than its floor.",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "//gate:lit-a11y": "Accessibility lint over the Lit templates. Most rules are error and clean; the three that are not are pinned by a one-way ratchet so known-bad cannot grow. --ratchet tightens after a fix.",
    "gate:lit-a11y": "node scripts/check-lit-a11y-ratchet.mjs",
    "lit-a11y:ratchet": "node scripts/check-lit-a11y-ratchet.mjs --ratchet",
    "//gate:coverage": "V8 code coverage against the one-way floor in .altitude/baselines/coverage.json. NOT the same thing as scripts/check-cem-coverage.js, which measures custom-elements-manifest completeness. Run test:unit:coverage first.",
    "gate:coverage": "node scripts/check-coverage-ratchet.mjs",
    "coverage:ratchet": "node scripts/check-coverage-ratchet.mjs --ratchet",
    "release": "pnpm run build && changeset publish",
    "//lint:styles": "THE HARD STYLE GATE. Stylelint over every .scss file in both component libraries and every example app (143 files, 0 violations as of 2026-08-23). Two rules only, both aimed at generated CSS: declaration-strict-value forces a var() on colour properties, value-no-unknown-custom-properties catches a hallucinated token name against the committed tokens digest. See stylelint.config.mjs.",
    "lint:styles": "stylelint \"libs/**/*.scss\" \"apps/**/*.scss\"",
    "//lint:styles:report": "The same rules over the sites' hand-written .css as well. NOT a gate: 21 pre-existing violations live there (15 literal status hues in apps/docs/src/styles/docs.css, 5 in the visual-compare harness, 1 inline-set swatch property in the southleft site). They are reported, not disabled — see scripts/check-styles-changed.mjs.",
    "lint:styles:report": "stylelint \"libs/**/*.scss\" \"apps/**/*.scss\" \"apps/**/*.css\" \".altitude/**/*.css\" || true",
    "lint:styles:fix": "stylelint \"libs/**/*.scss\" \"apps/**/*.scss\" --fix",
    "//gate:styles-new": "Stylelint on the lines a branch actually changed, in any .scss or .css file. This is what holds the line over the site CSS that lint:styles cannot gate. Per-PR: on a branch that has diverged far from main it approaches 'lint everything'.",
    "gate:styles-new": "node scripts/check-styles-changed.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check:publishable": "node scripts/check-publishable.mjs",
    "check:audit": "node scripts/check-audit.mjs",
    "//component:check": "Mechanical component-deliverable checklist for one tag (or --all): bundle.ts export, migration.json entry, CEM presence+freshness, source files (blockers); parity manifest, React wrapper, guidance YAML, llms.txt, changeset (warnings). The prose flow lives in .claude/skills/altitude-component-authoring/SKILL.md.",
    "component:check": "node scripts/component-check.mjs",
    "//parity:freshness": "Prints figmaLastRefreshed age, everObserved and unreachable statuses per DS project. Exit 0 by default (the Figma loop is deliberately operator-driven — .altitude/FIGMA-SYNC.md); --max-age-days N turns it into a gate. Wired as a warning-only CI step in repo-hygiene.",
    "parity:freshness": "node scripts/check-parity-freshness.mjs",
    "//parity:tokens-drift": "Token-level Figma<->code drift: value comparison (color/dimension/shadow canonicalization), brand/mode-aware bucketing, rename detection. Takes a saved figma_export_tokens JSON: pnpm run parity:tokens-drift -- .altitude/figma-sync/last-export.json [--project <id>]. Exit 1 on any drift. The export-shape assumption is best-effort until a real export is captured — see the script header.",
    "parity:tokens-drift": "node scripts/check-figma-drift.mjs",
    "check:mcp-docs": "node scripts/check-mcp-docs.mjs",
    "//check:brand-conformance": "For each brandLibrary supersession (brand al-header/al-footer capturing base tags), fails if base public surface (slots/parts/cssproperties/attributes) is missing from the brand override; also reports (never fails) base components the project's scope has never considered. Warning-only in CI until the real al-header/al-footer gaps are fixed — see the open issue.",
    "check:brand-conformance": "node scripts/check-brand-conformance.mjs",
    "//check:mcp-docs": "Asserts every hand-written surface that lists the Altitude MCP's tools (AGENTS.md, the MCP README, the southleft.com tools page) matches what server.mjs registers — the roster drifted to 'six tools' in three places while the server registered eight. check:llms guards the generated llms.txt; this guards the prose.",
    "llms:build": "node scripts/build-root-llms.mjs",
    "//check:llms": "The root llms.txt is GENERATED (scripts/build-root-llms.mjs) from the committed artifacts — the CEM, the two ai-readiness digests, the axe report, the ds-project registry and the MCP server's registerTool calls. This fails when it has drifted from them, and also when any repo-relative link it emits is missing or untracked, which is what put three dead build-artifact links in the hand-written version.",
    "check:llms": "node scripts/build-root-llms.mjs --check",
    "//check:llms-content-type": "Asserts the deployed machine artifacts arrive as text/plain and are not SPA shells. Takes a base URL; it is NOT part of any local gate, because a check that passes when nothing is deployed is the lie it exists to catch. Run it against a preview or production URL after a deploy.",
    "check:llms-content-type": "node scripts/check-llms-content-type.mjs",
    "check:token-usage": "node scripts/check-token-usage.mjs",
    "//gate:token-usage": "Fails on PHANTOM tokens only — a name read through var() that the pipeline never emits, so it silently falls back and its value is outside design-system control. DEAD tokens stay a report (see the script header): a tier-1 palette reserve is a legitimate design choice, a phantom never is. Reads styles/dist/tokens.json, so `build:tokens` is enough — no full build needed.",
    "gate:token-usage": "node scripts/check-token-usage.mjs --fail-on-phantom",
    "//generate:token-metadata": "Spec 2026-08-22-token-debt-and-machine-readable-metadata (R7/R8/R9). MUTATES libs/al-web-components/styles/tokens-dtcg/**.json (the hand-authored DTCG source), writing a `$extensions` block per token. IDENTITY fields are read back and never recomputed: com.adobe.id uuid (generated once), com.atlassian.token introduced (a historical fact that must survive a file move — re-deriving it from git history at a renamed path silently resets it), and org.altitude.token cssType (the CSS surface the token is authored for; NOT recoverable from DTCG $type, which collapses sizing/spacing/borderRadius/borderWidth/fontSizes/lineHeights all into `dimension`). DERIVED fields are recomputed every run: org.primer.llm usage rules (authored per FAMILY, ~72 of them, inherited by every member — 552+ per-token prose would not be truthful), com.salesforce.styling cssProperties (derived from cssType, never fabricated), and com.atlassian.token state/replacement (state is only ever \"deprecated\" for the small, individually-verified list in scripts/lib/token-metadata-rules.mjs). A token with no cssType gets no cssProperties and is reported at the end of the run. Idempotent — safe to rerun after adding a token. See scripts/lib/dtcg-token.mjs for the two-types-per-token model, and gate:token-metadata for what enforces uuid stability.",
    "generate:token-metadata": "node scripts/generate-token-metadata.mjs",
    "//check:token-metadata": "THE drift gate for the metadata generate:token-metadata writes — a generated artifact with no drift gate is the exact failure this spec keeps finding elsewhere. Asserts every token has a uuid, no two tokens share one, no existing token's uuid differs from what it was at HEAD (compares against `git show HEAD:<file>`, so there is no separate baseline file to rebaseline), and every `replacement` dot-path resolves to a real token. Run after generate:token-metadata or any manual edit to styles/tokens-dtcg/**.json. NOTE: files absent from HEAD compare against nothing — so immediately after the tokens/ -> tokens-dtcg/ move the uuid comparison reports 0 tokens and is a no-op. It re-arms on the first commit that contains the new paths.",
    "check:token-metadata": "node scripts/check-token-metadata.mjs",
    "//codemod:deprecated-tokens": "Reads every `state: \"deprecated\"` token's `replacement` and scans real source for var() call sites. Same-CSS-property replacements (e.g. a duplicate alias) are rewritten with --write; cross-property replacements (e.g. a colour token superseded by an opacity token) are always just reported — a codemod that silently changes which CSS property a component sets is not safe. Dry run by default.",
    "codemod:deprecated-tokens": "node scripts/codemod-deprecated-tokens.mjs",
    "//skills:build": "Regenerates .claude/skills/altitude-facts/GENERATED-FACTS.md (R10) — the mechanical half of the altitude-facts agent skill: component roster, per-tag doNotFlag caveats, token conventions/do-not-invent list and the MCP tool/resource/prompt roster, all read from the same committed artifacts llms.txt and the MCP capability matrix already read. The judgement half (SKILL.md) stays hand-written; see its §0.",
    "skills:build": "node scripts/build-agent-skill.mjs",
    "//check:skills": "Fails when .claude/skills/altitude-facts/GENERATED-FACTS.md has drifted from its sources — same discipline as check:llms and check:mcp-docs, applied to the agent skill instead of a doc page.",
    "check:skills": "node scripts/build-agent-skill.mjs --check"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@eslint/js": "^9",
    "@open-wc/testing": "^4.0.0",
    "@open-wc/testing-helpers": "^3.0.1",
    "@playwright/test": "^1.42.0",
    "@vitest/browser": "3.2.4",
    "@vitest/coverage-v8": "3.2.4",
    "ajv": "^8",
    "axe-core": "^4.10",
    "eslint": "^9",
    "eslint-plugin-lit-a11y": "^5.1.1",
    "globals": "^15",
    "pixelmatch": "^7.2.0",
    "playwright": "^1.42.0",
    "pngjs": "^7.0.0",
    "postcss-scss": "^4",
    "sass": "^1.101",
    "stylelint": "^17",
    "stylelint-declaration-strict-value": "^1.11.1",
    "stylelint-value-no-unknown-custom-properties": "^6.1.1",
    "typescript": "^5.7",
    "typescript-eslint": "^8",
    "vitest": "3.2.4"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.15.0"
  },
  "author": "Southleft, LLC"
}
