{
  "name": "@southleft/altitude",
  "description": "Design system created by Southleft.com",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "libs/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspace al-web-components build",
    "build:storybook-web-components": "yarn workspace al-web-components build:storybook",
    "build:storybook-react": "yarn workspace al-react build:storybook",
    "build:app-angular": "yarn workspace al-app-angular build",
    "build:all": "yarn build && yarn build:storybook-web-components && yarn build:storybook-react && yarn build:app-angular"
  }
}