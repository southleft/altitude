{
  "name": "al-app-mfe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "T0.3 fixture: micro-frontend coexistence harness. Renders two simulated Altitude consumers side-by-side so T4.6 can prove the `versioned` registry mode keeps them isolated.",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5176"
  },
  "dependencies": {
    "al-web-components": "1.0.0"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}
