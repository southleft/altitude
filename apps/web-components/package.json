{
  "name": "al-app-web-components",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "T0.3 fixture: minimal Lit consumer that imports the 5 pilot web components. Used as an acceptance test for later phases.",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5174"
  },
  "dependencies": {
    "al-web-components": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
