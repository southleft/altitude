<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-testimonial.contract.json + the southleft parity manifest — do not hand-edit; run `pnpm contracts:docs:sl` -->

# al-testimonial — Testimonial

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<figure>`

Component: al-testimonial

A single customer quote with attribution — for marketing pages (social
proof rows, case-study pull-quotes). Composes `<al-avatar>` via the
`avatar` slot for a headshot.

## Figma

- Component set: **Testimonial**
- Node id: `19:2589` (pinned)
- [Open in Figma](https://www.figma.com/design/2a0kqTG8i8l1q1VI2yq9or/?node-id=19-2589)

## Props (3)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `attribution` | string | — | — | — |
| `attribution-role` | string | — | — | — |
| `company` | string | — | — | — |

#### `attribution`

The name of the person being quoted.

#### `attribution-role`

Job title of the person being quoted, e.g. "VP of Engineering".

The attribute is `attribution-role`, **not** `role`. `role` is the ARIA
role attribute: `<al-testimonial role="VP of Engineering">` put a job title
where the accessibility tree expects a role name, and axe reported
`aria-roles` ("role attribute must use a valid value") on every
Testimonial story. `apps/southleft` had already worked around it by
writing the *property* from a `data-testimonial-role` attribute.

#### `company`

Company/organization of the person being quoted.

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `focus`, `active`, `disabled`

## Slots (2)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The quote text. | — | — |
| `avatar` | Optional avatar (typically an `<al-avatar>`). | — | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `` (source: `measured`)

### Root — `<figure class="al-c-testimonial">`

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| column-gap | `--al-theme-space-md` | `theme/space/md` |
| gap | `--al-theme-space-md` | `theme/space/md` |
| padding-left | `--al-theme-space-md` | `theme/space/md` |
| row-gap | `--al-theme-space-md` | `theme/space/md` |

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/testimonial/testimonial.ts`
- Tag: `al-testimonial`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (10)

`--al-font-weight-bold`, `--al-theme-color-content-default`, `--al-theme-color-content-default-weak`, `--al-theme-space-md`, `--al-theme-space-sm`, `--al-theme-typography-body-md-bold-letter-spacing`, `--al-theme-typography-body-sm`, `--al-theme-typography-body-sm-letter-spacing`, `--al-theme-typography-heading-sm`, `--al-theme-typography-heading-sm-letter-spacing`

---

Source contract: `.altitude/contracts/southleft/al-testimonial.contract.json`.
