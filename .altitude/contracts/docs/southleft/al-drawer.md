<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-drawer.contract.json + the southleft parity manifest — do not hand-edit; run `pnpm contracts:docs:sl` -->

# al-drawer — Drawer

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-drawer

## Figma

No Figma component set is mapped for `al-drawer` in the **Southleft Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed --project southleft`.

## Props (6)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `alignment` | string | `'right'` | — | — |
| `ariaLabelledBy` | string | — | — | — |
| `disableBackdropClick` | boolean | — | — | — |
| `hasBackdrop` | boolean | — | — | — |
| `isActive` | boolean | — | — | — |
| `width` | number | — | — | — |

#### `alignment`

Alignment
- **right** Aligns the drawer to the right of the viewport

#### `ariaLabelledBy`

Aria Labelled By attribute
- Dynamically set for A11y

#### `disableBackdropClick`

Disable to close the drawer on outside click

#### `hasBackdrop`

Fade/ghost back the page background content

#### `isActive`

Is active?
- **true** Shows the drawer container
- **false** Hides the drawer container

#### `width`

Width of drawer panel when expanded / collapsed as side panel

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `focus`, `active`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The drawer content | — | — |

## Events (3)

| Name | Description |
| --- | --- |
| `onDrawerClose` | Fired when the drawer closes by any means. Detail: `{ active }`. |
| `onDrawerCloseButton` | Fired only when the drawer's close button is activated. Detail: `{ active }`. Use `onDrawerClose` to catch every close path. |
| `onDrawerOpen` | Fired when the drawer opens. Detail: `{ active }` — the new open state. |

## Accessibility

- ARIA-bearing attributes: `ariaLabelledBy`
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Alignment=bottom` (source: `measured`)

### Root — `<div class="al-c-drawer al-is-active">`

_None._

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/drawer/drawer.ts`
- Tag: `al-drawer`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (10)

`--al-base-space`, `--al-font-weight-bold`, `--al-theme-border-radius-role-action`, `--al-theme-box-shadow-md`, `--al-theme-color-background-default`, `--al-theme-color-background-transparent-default`, `--al-theme-color-content-default`, `--al-theme-space`, `--al-theme-space-xs`, `--al-theme-typography-body-md-bold-letter-spacing`

---

Source contract: `.altitude/contracts/southleft/al-drawer.contract.json`.
