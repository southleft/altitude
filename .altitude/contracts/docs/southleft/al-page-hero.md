<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-page-hero.contract.json + the southleft parity manifest — do not hand-edit; run `pnpm contracts:docs:sl` -->

# al-page-hero — Page Hero

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-page-hero

Southleft's INTERIOR-page hero band — an eyebrow, a display heading, a lead
paragraph, and whatever the page adds below. Appears on 19 of the site's 24
pages.

Not to be confused with `al-hero`, the HOMEPAGE hero: that one is a
two-column landing composition with the grid texture behind it. They are
genuinely different shapes, and the site's own class names (`.al-page-hero`
vs `.al-hero`) already tell them apart — this keeps that distinction rather
than overloading one name with two jobs.

What it owns is the BAND: the vertical rhythm, and the eyebrow hugging its
content instead of stretching. Arrangement is `<al-layout>`'s job, and the
stack below is exactly that — nested in the shadow root so the page does not
have to restate it, not replaced by a grid of this component's own.

```html
<al-page-hero label="Services" heading="Design systems that hold" dek="…">
  <al-layout direction="row" gap="md">
    <al-button>Book a call</al-button>
  </al-layout>
</al-page-hero>
```

The three content props are CONTENT, not layout — they say what the band
says, never where anything sits. A fundamentally different hero shape is a
different component, not another prop on this one. That distinction is the
whole reason `al-hero` was removed from Altitude: six fixed named slots plus
five layout props, abandoned mid-build by its first real consumer.

## Figma

- Component set: **Page Hero**
- Node id: `10:3334` (pinned)
- [Open in Figma](https://www.figma.com/design/rdhBS9t89V42E7EfiPjmSa/?node-id=10-3334)

## Props (6)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `align` | enum | `center`, `start` | `'start'` | — |
| `dek` | string | — | — | — |
| `heading` | string | — | — | — |
| `heading-tag` | string | — | `'h1'` | — |
| `heading-variant` | string | — | `'display-md'` | — |
| `label` | string | — | — | — |

#### `align`

Horizontal alignment of the whole band. `start` is the default; `center` is
what the 404 and quiz pages want, and it has to move the EYEBROW too — the
chip wrapper pins `align-self: start`, so text-align alone would leave it
hanging off to the left.

#### `dek`

The lead paragraph under the heading. Omit for none.

#### `heading`

The band's heading.

#### `heading-tag`

The heading's semantic level. `h1` on a page's primary hero, `h2` where the
band is a section rather than the page title — a document-outline concern,
which is why it is not fixed.

#### `heading-variant`

Heading scale. `display-md` is the interior-page default; `display-sm` is what
an article or project band uses inside the narrower prose measure. Without
this the four hand-rolled title bands on this site could not adopt the
component at all — the variant was hardcoded.

#### `label`

The eyebrow chip above the heading. Omit for no eyebrow.

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `focus`, `active`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | Anything below the lead paragraph — CTAs, a token strip, a form. Laid out as further children of the stack. | — | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: `heading-variant`
- CSS parts: `band`, `dek`, `eyebrow`, `heading`

## Anatomy & token bindings

**Anatomy case measured:** `` (source: `measured`)

### Root — `<div class="sl-c-page-hero">`

_None._

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/sl-web-components/components/page-hero/page-hero.ts`
- Tag: `al-page-hero`
- Workspace: `@southleft/sl-web-components`

## Tokens referenced (16)

`--al-base-space`, `--al-font-weight-bold`, `--al-theme-border-width`, `--al-theme-color-background-default-strong`, `--al-theme-color-background-transparent-default`, `--al-theme-color-border-default`, `--al-theme-color-content-default`, `--al-theme-color-content-default-weak`, `--al-theme-space`, `--al-theme-space-sm`, `--al-theme-space-xs`, `--al-theme-space-xxs`, `--al-theme-typography-body-sm-letter-spacing`, `--al-theme-typography-display-md`, `--al-theme-typography-display-md-bold-letter-spacing`, `--al-typography-preset-16`

---

Source contract: `.altitude/contracts/southleft/al-page-hero.contract.json`.
