<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-logo-wall.contract.json + the southleft parity manifest — do not hand-edit; run `pnpm contracts:docs:sl` -->

# al-logo-wall — Logo Wall

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-logo-wall

The client logo band — a wrapping row of monochrome marks that come up to
full strength on hover.

```html
<al-logo-wall>
  <img src="/logos/ibm.webp" alt="IBM" />
  <img src="/logos/google.webp" alt="Google" />
</al-logo-wall>
```

The marks are slotted, because which clients appear is the page's business.
What the component owns is the treatment: the uniform optical height, the
knock-back-and-reveal, and the asymmetric row/column rhythm the brand uses.

## Figma

- Component set: **Logo Wall**
- Node id: `10:3386` (pinned)
- [Open in Figma](https://www.figma.com/design/rdhBS9t89V42E7EfiPjmSa/?node-id=10-3386)

## Props (1)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `vivid` | boolean | — | — | **Vivid** (VARIANT): `Default`, `Vivid` |

#### `vivid`

Render the marks at full strength instead of knocked back. For a page where
the logos are the subject rather than social proof in passing.

## Variant axes

### `vivid` (Figma property "Vivid")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `Default`, `Vivid`

## States

`hover`, `focus`, `active`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The logo images. Sized and treated by the component; pass them at their natural dimensions. | — | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: `wall`

## Anatomy & token bindings

**Anatomy case measured:** `Vivid=default` (source: `measured`)

### Root — `<div class="sl-c-logo-wall">`

_None._

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/sl-web-components/components/logo-wall/logo-wall.ts`
- Tag: `al-logo-wall`
- Workspace: `@southleft/sl-web-components`

## Tokens referenced (1)

`--al-layout-gap`

---

Source contract: `.altitude/contracts/southleft/al-logo-wall.contract.json`.
