<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-header.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-header — Header

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Component: al-header

The site header landmark and its chrome. Header owns the `<header>` element,
the opt-in sticky/elevated behaviour and the stacking context. Arrangement is
`<al-layout>`'s job — nest one and lay the bar out there.

```html
<al-header sticky elevated>
  <al-layout direction="row" align="center" justify="between">
    <al-logo></al-logo>
    <al-menu>...</al-menu>
    <al-layout direction="row" gap="sm">...actions...</al-layout>
  </al-layout>
</al-header>
```

See "Arrangement vs. semantics" in AGENTS.md.

## Figma

No Figma component set is mapped for `al-header` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (2)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `elevated` | boolean | — | — | — |
| `sticky` | boolean | — | — | — |

#### `elevated`

Cast a shadow under the bar to separate it from the content it scrolls over.

#### `sticky`

Stick the header to the top of its scroll container.

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (1)

| Slot | Description | Figma placeholder |
| --- | --- | --- |
| `(default)` | The header content. Wrap it in an `<al-layout>` to arrange it. | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/header/header.ts`
- Tag: `al-header`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-header.contract.json`.
