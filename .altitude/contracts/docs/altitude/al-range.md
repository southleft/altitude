<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-range.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-range — Range

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-range

## Figma

- Component set: **Range**
- Node id: `3558:61875` (pinned)
- [Open in Figma](https://www.figma.com/design/y83n4o9LOGs74oAoguFcGS/?node-id=3558-61875)

## Props (21)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `ariaDescribedBy` | string | — | — | _not expressed in Figma (by design)_ |
| `behavior` | string | `'range'` | — | **Behavior** (VARIANT): `Default`, `Range` |
| `errorNote` | string | — | — | _not expressed in Figma (by design)_ |
| `fieldId` | string | — | — | _not expressed in Figma (by design)_ |
| `fieldNote` | string | — | — | _not expressed in Figma (by design)_ |
| `handleLabelUnit` | string | — | — | _not expressed in Figma (by design)_ |
| `hasOutput` | boolean | — | — | **Output** (VARIANT): `No`, `Yes` |
| `hasTooltip` | boolean | — | — | — |
| `hideLabel` | boolean | — | — | — |
| `isDisabled` | boolean | — | — | — |
| `isError` | boolean | — | — | — |
| `label` | string | — | — | _not expressed in Figma (by design)_ |
| `lowerRangeValue` | number | — | — | _not expressed in Figma (by design)_ |
| `max` | number | — | `100` | _not expressed in Figma (by design)_ |
| `maxSelected` | number | — | — | _not expressed in Figma (by design)_ |
| `min` | number | — | `0` | _not expressed in Figma (by design)_ |
| `minSelected` | number | — | — | _not expressed in Figma (by design)_ |
| `name` | string | — | — | _not expressed in Figma (by design)_ |
| `step` | number | — | `1` | _not expressed in Figma (by design)_ |
| `upperRangeValue` | number | — | — | _not expressed in Figma (by design)_ |
| `value` | number | — | — | _not expressed in Figma (by design)_ |

#### `ariaDescribedBy`

Aria described by
- Used to connect the field note in select to the select menu for accessibility

#### `behavior`

Behavior property for slide

#### `errorNote`

Error note text

#### `fieldId`

The unique id of the field

#### `fieldNote`

Field note text

#### `handleLabelUnit`

Handle label unit attribute

#### `hasOutput`

Shows input value

#### `hasTooltip`

Displays value label above range

#### `hideLabel`

Hide label?
- If true, hides the label from displaying

#### `isDisabled`

Disabled attribute

#### `isError`

Error state

#### `label`

Slide Label

#### `lowerRangeValue`

Input value for lower range of range

#### `max`

Max property for range

#### `maxSelected`

Maxselected property for range

#### `min`

Min property for range

#### `minSelected`

Minselected property for range

#### `name`

Name attribute forwarded to the underlying `<input type="range">`.
- Previously hardcoded to `"range"`, which silently overrode whatever the
  consumer submitted the field as.

#### `step`

Step for range

#### `upperRangeValue`

Input value for upper range of range

#### `value`

Input value

**Figma-expression opt-out (T27):** `ariaDescribedBy`, `errorNote`, `fieldId`, `fieldNote`, `handleLabelUnit`, `label`, `lowerRangeValue`, `max`, `maxSelected`, `min`, `minSelected`, `name`, `step`, `upperRangeValue`, `value` are curated `bindings.figma.omit: true` — a deliberate decision to keep this prop out of the generated Figma set entirely (no axis, no component property, no instance), independent of whether the real set happens to expose one today. See `.altitude/contracts/README.md` § Figma-expression opt-out.

## Variant axes

### `behavior` (Figma property "Behavior")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `Default`, `Range`

### `hasOutput` (Figma property "Output")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `No`, `Yes`

## States

`hover`, `focus`, `active`, `disabled`

## Slots (3)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `after` | If content is slotted, it will override the default range "max" label text | — | — |
| `before` | If content is slotted, it will override the default range "min" label text | — | — |
| `label` | If content is slotted, it will override the default range label | — | — |

## Events (2)

| Name | Description |
| --- | --- |
| `onRangeDrag` | Fired continuously while the thumb is dragged. Prefer `onRangeOutputValueChange` for committed values; this one fires on every move. |
| `onRangeOutputValueChange` | Fired when the range's output value changes. Detail: `{ value }`. |

## Accessibility

- ARIA-bearing attributes: `ariaDescribedBy`
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Behavior=default,Output=no` (source: `measured`)

### Root — `<div class="al-c-range">`

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| column-gap | `--al-theme-space-xxs` | `theme/space/xxs` |
| gap | `--al-theme-space-xxs` | `theme/space/xxs` |
| row-gap | `--al-theme-space-xxs` | `theme/space/xxs` |

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/range/range.ts`
- Tag: `al-range`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (12)

`--al-range-track-height`, `--al-theme-border-radius`, `--al-theme-color-background-neutral-strong`, `--al-theme-color-content-neutral-default`, `--al-theme-color-content-neutral-weak`, `--al-theme-space`, `--al-theme-space-xs`, `--al-theme-space-xxs`, `--al-theme-typography-body-md`, `--al-theme-typography-body-md-letter-spacing`, `--al-theme-typography-body-sm`, `--al-theme-typography-body-sm-letter-spacing`

---

Source contract: `.altitude/contracts/altitude/al-range.contract.json`.
