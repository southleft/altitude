<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-date-time-picker.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-date-time-picker — Date Time Picker

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Component: al-date-time-picker

## Figma

No Figma component set is mapped for `al-date-time-picker` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (33)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `ariaDescribedBy` | string | — | — | — |
| `cancelLabel` | string | — | `'Cancel'` | — |
| `dateFormat` | enum | `MM/dd/yyyy`, `MMM dd yyyy`, `MMM dd, yyyy`, `dd MMM yyyy`, `dd/MM/yyyy`, `yyyy/MM/dd` | `'MMM dd, yyyy'` | — |
| `disabledMaxDate` | string | `any` | — | — |
| `disabledMinDate` | string | `any` | — | — |
| `errorNote` | string | — | — | — |
| `fieldId` | string | — | — | — |
| `fieldNote` | string | — | — | — |
| `hideLabel` | boolean | — | — | — |
| `is24HourFormat` | boolean | — | — | — |
| `isActive` | boolean | — | — | — |
| `isDayShortHand` | boolean | — | — | — |
| `isDisabled` | boolean | — | — | — |
| `isError` | boolean | — | — | — |
| `isoFormat` | string | — | `'yyyy-MM-dd'` | — |
| `isOptional` | boolean | — | — | — |
| `isRequired` | boolean | — | — | — |
| `label` | string | — | `'Date and Time'` | — |
| `multiYear` | number | — | `3` | — |
| `name` | string | — | — | — |
| `nextButtonText` | string | — | `'Next Month'` | — |
| `placeholder` | string | — | — | — |
| `previousButtonText` | string | — | `'Previous Month'` | — |
| `selectedDate` | string | `any` | — | — |
| `setActiveDate` | string | `any` | — | — |
| `setActiveTime` | string | `any` | — | — |
| `startOnMonday` | boolean | — | — | — |
| `submitLabel` | string | — | `'Apply'` | — |
| `timeEnd` | number | — | — | — |
| `timeIncrements` | number | — | `30` | — |
| `timeSelectorLabel` | string | — | `'Time'` | — |
| `timeStart` | number | — | — | — |
| `value` | string | — | — | — |

#### `ariaDescribedBy`

Aria describedby
- Used to connect the field note in the field to the input for accessibility

#### `cancelLabel`

Cancel button label

#### `dateFormat`

Specify date format for UI display

#### `disabledMaxDate`

Maximum date for disabled dates (format: yyyy/mm/dd)

#### `disabledMinDate`

Minimum date for disabled dates (format: yyyy/mm/dd)

#### `errorNote`

Error note displayed beneath the field when in an error state

#### `fieldId`

The unique id of the field

#### `fieldNote`

The field note displayed beneath the field

#### `hideLabel`

Hide label?

#### `is24HourFormat`

Use 24 hour time format

#### `isActive`

Indicates whether a date has been selected

#### `isDayShortHand`

Show the day of the week as a short hand, e.g. "M" for Monday

#### `isDisabled`

The field's disabled attribute

#### `isError`

Error state indicating an issue with the field

#### `isoFormat`

ISO Format for date

#### `isOptional`

Optional state
- Specifies that a field is optional and adds the text 'optional' to the label

#### `isRequired`

The field's required attribute

#### `label`

The field's label

#### `multiYear`

Amount of years to go before and after the current date

#### `name`

The field's name attribute

#### `nextButtonText`

Next month button text

#### `placeholder`

Placeholder attribute
- Specifies a short hint that describes the expected value of an input element

#### `previousButtonText`

Previous month button text

#### `selectedDate`

Holds the selected date value

#### `setActiveDate`

Set a specific date to be active

#### `setActiveTime`

Set a specific time to be active (24 hour format, e.g., setActiveTime="16:00" for 4PM)

#### `startOnMonday`

Start the day of the week on Monday

#### `submitLabel`

Submit button label

#### `timeEnd`

Maximum time for between times display (24 hour format, e.g., timeEnd=${16} for 4PM)

#### `timeIncrements`

Increments of time

#### `timeSelectorLabel`

Time selector heading label

#### `timeStart`

Minimum time for between times display (24 hour format, e.g., timeStart=${16} for 4PM)

#### `value`

The field's value attribute

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (2)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `error` | If content is slotted, it will display in place of the errorNote property | — | — |
| `field-note` | If content is slotted, it will display in place of the fieldNote property | — | — |

## Events (4)

| Name | Description |
| --- | --- |
| `onDateTimePickerClose` | Fired when the calendar overlay closes. Detail: `{ activeCalendar }` — the new open state. |
| `onDateTimePickerDateChange` | Fired when the date portion changes. Detail: `{ value }` — the formatted date-time string. Fires independently of the time portion. |
| `onDateTimePickerOpen` | Fired when the calendar overlay opens. Detail: `{ activeCalendar }` — the new open state. |
| `onDateTimePickerTimeChange` | Fired when the time portion changes. Detail: `{ value }` — the formatted date-time string. Fires independently of the date portion. |

## Accessibility

- ARIA-bearing attributes: `ariaDescribedBy`
- CSS parts: —

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/date-time-picker/date-time-picker.ts`
- Tag: `al-date-time-picker`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-date-time-picker.contract.json`.
