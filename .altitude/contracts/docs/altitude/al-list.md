<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-list.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-list — List

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<ul>`

Component: al-list

## Figma

No Figma component set is mapped for `al-list` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (2)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `behavior` | string | `'overflow'` | — | — |
| `direction` | string | `'row'` | — | — |

#### `behavior`

Behavior
- **overflow** turns the list into an overflow list

#### `direction`

Orientation
- default renders a stacked list
- **horizontal** renders the list side-by-side and wraps unless overflow is turned on

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `focus`, `active`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The list items | — | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Direction=default` (source: `measured`)

### Root — `<ul class="al-c-list">`

_None._

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/list/list.ts`
- Tag: `al-list`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (5)

`--al-list-item-link-padding-inline-start`, `--al-theme-space-xs`, `--al-theme-space-xxs`, `--al-theme-typography-body-sm`, `--al-theme-typography-body-sm-letter-spacing`

---

Source contract: `.altitude/contracts/altitude/al-list.contract.json`.
