<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-focus-trap.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-focus-trap — Focus Trap

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Component: al-focus-trap

## Figma

No Figma component set is mapped for `al-focus-trap` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (2)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `isActive` | boolean | — | — | — |
| `transitionDelay` | number | — | `0` | — |

#### `isActive`

Is the focus trap active?

#### `transitionDelay`

Delay in milliseconds before the focus trap is activated.
- Can be set by the component using focus trap, when there is a css transition that needs to complete before the focus trap activates.

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (1)

| Slot | Description | Figma placeholder |
| --- | --- | --- |
| `(default)` | The content to trap in the focus trap | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/focus-trap/focus-trap.ts`
- Tag: `al-focus-trap`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-focus-trap.contract.json`.
