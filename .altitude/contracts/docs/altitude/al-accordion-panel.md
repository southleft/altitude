<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-accordion-panel.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-accordion-panel — Accordion Panel

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-accordion-panel

## Figma

No Figma component set is mapped for `al-accordion-panel` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (6)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `ariaControls` | string | — | — | — |
| `ariaLabelledBy` | string | — | — | — |
| `idx` | number | — | `0` | — |
| `isActive` | boolean | — | — | — |
| `isDisabled` | boolean | — | — | — |
| `isLast` | boolean | — | — | — |

#### `ariaControls`

Aria Controls attribute
- Dynamically set for A11y

#### `ariaLabelledBy`

Aria Labelled By attribute
- Dynamically set for A11y

#### `idx`

Index of the panel
- Dynamically set by the ALAccordion

#### `isActive`

Active property
1. Panel is open when set to true. Close when set to false

#### `isDisabled`

Disabled property

#### `isLast`

Is last?
- Dynamically set by the ALAccordion
- Removes the bottom border if it is the last item

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The accordion panel content | — | — |

## Events (2)

| Name | Description |
| --- | --- |
| `onAccordionPanelClose` | Fired when the panel collapses. Detail: `{ index, expanded }` — the panel's index within the accordion and its new expanded state. |
| `onAccordionPanelOpen` | Fired when the panel expands. Detail: `{ index, expanded }` — the panel's index within the accordion and its new expanded state. |

## Accessibility

- ARIA-bearing attributes: `ariaControls`, `ariaLabelledBy`
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Expanded=No,Position=last` (source: `measured`)

### Root — `<div class="al-c-accordion-panel al-is-last">`

_None._

### State overrides (measured, root node)

**`hover`**

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| background-color | `--al-theme-color-background-default` | `theme/color/background/default` |

**`disabled`**

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| opacity | `--al-theme-opacity-disabled` | `theme/opacity/disabled` |

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/accordion-panel/accordion-panel.ts`
- Tag: `al-accordion-panel`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (7)

`--al-font-weight-bold`, `--al-theme-color-background-default`, `--al-theme-color-background-transparent-default`, `--al-theme-icon-lg`, `--al-theme-opacity-disabled`, `--al-theme-space`, `--al-theme-typography-body-sm-bold-letter-spacing`

---

Source contract: `.altitude/contracts/altitude/al-accordion-panel.contract.json`.
