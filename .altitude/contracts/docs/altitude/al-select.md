<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-select.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-select — Select

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Component: al-select

## Figma

No Figma component set is mapped for `al-select` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (19)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `align` | enum | `bottom`, `top` | `'bottom'` | — |
| `ariaDescribedBy` | string | — | — | — |
| `errorNote` | string | — | — | — |
| `fieldId` | string | — | — | — |
| `fieldNote` | string | — | — | — |
| `hasSearch` | boolean | — | `false` | — |
| `hideLabel` | boolean | — | — | — |
| `isActive` | boolean | — | — | — |
| `isActiveDropdown` | boolean | — | — | — |
| `isDisabled` | boolean | — | — | — |
| `isError` | boolean | — | — | — |
| `isOptional` | boolean | — | — | — |
| `isReadonly` | boolean | — | `true` | — |
| `isRequired` | boolean | — | `false` | — |
| `label` | string | — | `'Label'` | — |
| `name` | string | — | — | — |
| `placeholder` | string | — | — | — |
| `title` | string | — | — | — |
| `value` | string | — | — | — |

#### `align`

**Select alignment**
- **bottom** Dropdown panel appears on the bottom
- **top** Dropdown panel appears on the top

#### `ariaDescribedBy`

Aria describedby
1. Used to connect the field note in select to the select menu for accessibility

#### `errorNote`

The select error note

#### `fieldId`

The unique id of the select

#### `fieldNote`

The select field note

#### `hasSearch`

Add a search input to the dropdown panel

#### `hideLabel`

Hide the label?

#### `isActive`

is Active

#### `isActiveDropdown`

is Active select
1. Select is open when set to true. Close when set to false

#### `isDisabled`

The select's disabled attribute

#### `isError`

Error state

#### `isOptional`

Optional state
- Specifies that a field is optional and adds the text 'optional' to the label

#### `isReadonly`

Readonly attribute
- Specifies that an input field is read-only

#### `isRequired`

The select's required attribute

#### `label`

The select's label

#### `name`

The select's name attribute

#### `placeholder`

Placeholder attribute
- Specifies a short hint that describes the expected value of an input element

#### `title`

The select's title

#### `value`

The select's value attribute

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (3)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `(default)` | The select content | — | — |
| `error` | If content is slotted, it will display in place of the errorNote property | — | — |
| `field-note` | If content is slotted, it will display in place of the fieldNote property | — | — |

## Events (2)

| Name | Description |
| --- | --- |
| `onSelectClose` | Fired when the option list closes. Detail: `{ active }` — always `false`. |
| `onSelectOpen` | Fired when the option list opens. Detail: `{ active }` — always `true`. |

## Accessibility

- ARIA-bearing attributes: `ariaDescribedBy`
- CSS parts: —

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/select/select.ts`
- Tag: `al-select`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-select.contract.json`.
