<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-dropdown-panel.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-dropdown-panel — Dropdown Panel

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-dropdown-panel

## Figma

No Figma component set is mapped for `al-dropdown-panel` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (3)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `hasFooter` | boolean | — | `false` | — |
| `hasHeader` | boolean | — | `false` | — |
| `hasScroll` | boolean | — | — | — |

#### `hasFooter`

Changes styling on panel for variant with footer

#### `hasHeader`

Changes styling on panel for variant with header

#### `hasScroll`

Adds a max height and scrolling of the panel

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

`hover`, `focus`, `active`, `disabled`

## Slots (1)

| Slot | Description | Figma placeholder |
| --- | --- | --- |
| `(default)` | The dropdown panel content | — |

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Header=no,Footer=no` (source: `measured`)

### Root — `<div class="al-c-dropdown-panel">`

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| background-color | `--al-theme-color-background-default` | `theme/color/background/default` |
| box-shadow | `--al-theme-box-shadow-md` | `theme/box-shadow/md` |

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/dropdown-panel/dropdown-panel.ts`
- Tag: `al-dropdown-panel`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (3)

`--al-theme-box-shadow-md`, `--al-theme-color-background-default`, `--al-theme-space-xxs`

---

Source contract: `.altitude/contracts/altitude/al-dropdown-panel.contract.json`.
