<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-tabs.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-tabs — Tabs

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<div>`

Component: al-tabs

## Figma

- Component set: **Tabs**
- Node id: not pinned (`nodeId: null`) — resolve this set by **NAME** via the parity manifest (`.altitude/figma-sync/parity-manifest.json`) or the `altitude_check_parity` MCP tool at need. Node ids for by-name-mapped components are re-minted when their Figma page is rebuilt, so a value hard-coded here would go stale — see `.altitude/PARITY.md`.
- File: https://www.figma.com/design/y83n4o9LOGs74oAoguFcGS/

## Props (2)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `activeIndex` | number | — | — | — |
| `variant` | string | `'stretch'` | — | **Variant** (VARIANT): `Default`, `Stretch` |

#### `activeIndex`

Active tab state
- Sets the initial active tab (e.g. 0 sets the first tab, 1 sets the second tab, etc.)

#### `variant`

Tabs variant
- **default** Tabs are left-aligned, and the width of each tab is defined by the length of its content
- **stretch** Tabs stretch horizontally to have equal widths, which is calculated by the width of the screen divided by the number of tabs

## Variant axes

### `variant` (Figma property "Variant")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `Default`, `Stretch`

## States

`hover`, `focus`, `active`, `disabled`

## Slots (2)

| Slot | Description | Figma placeholder |
| --- | --- | --- |
| `(default)` | The tab items for the tabs | — |
| `panel` | The tab panels that correspond to the slotted tab items | — |

## Events (1)

| Name | Description |
| --- | --- |
| `onTabsChange` | Fired when the active tab changes, whether by user interaction or programmatically. Detail: `{ value, activeTabIdx }`. |

## Accessibility

- ARIA-bearing attributes: `variant`
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Variant=default` (source: `measured`)

### Root — `<div class="al-c-tabs">`

_None._

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/tabs/tabs.ts`
- Tag: `al-tabs`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (13)

`--al-base-space`, `--al-font-weight-bold`, `--al-theme-color-background-danger-default`, `--al-theme-color-background-transparent-default`, `--al-theme-color-content-danger-weak`, `--al-theme-color-content-default`, `--al-theme-color-content-default-weak`, `--al-theme-opacity-disabled`, `--al-theme-space`, `--al-theme-space-xs`, `--al-theme-space-xxs`, `--al-typography-preset-14`, `--al-typography-preset-16`

---

Source contract: `.altitude/contracts/altitude/al-tabs.contract.json`.
