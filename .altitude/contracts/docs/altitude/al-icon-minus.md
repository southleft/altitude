<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-icon-minus.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-icon-minus — Icon Minus

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Icon: al-icon-minus

## Figma

No Figma component set is mapped for `al-icon-minus` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (2)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `iconTitle` | string | — | — | — |
| `size` | enum | `lg`, `md`, `sm`, `xl`, `xs`, `xxl`, `xxxl` | — | — |

#### `iconTitle`

Accessible name. When set, the icon is exposed as `role="img"` with this
label; when omitted the icon is hidden from assistive technology, which is
correct for decorative icons sitting next to visible text.

#### `size`

Icon size
- Default size is 16px
- **xs** renders a 8px icon
- **sm** renders a 12px icon
- **md** renders a 20px icon
- **lg** renders a 24px icon
- **xl** renders a 32px icon
- **xxl** renders a 36px icon
- **xxxl** renders a 40px icon

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (0)

_No slots declared._

## Events (0)

_No events declared._

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: `svg`

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/icon/icons/minus.ts`
- Tag: `al-icon-minus`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-icon-minus.contract.json`.
