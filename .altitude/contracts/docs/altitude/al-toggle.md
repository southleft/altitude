<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-toggle.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-toggle — Toggle

**Status:** `source` · **Version:** `1.1.0` · **Element:** `<div>`

Component: al-toggle

## Figma

- Component set: **Toggle**
- Node id: `3543:48094` (pinned)
- [Open in Figma](https://www.figma.com/design/y83n4o9LOGs74oAoguFcGS/?node-id=3543-48094)

## Props (5)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `fieldId` | string | — | — | — |
| `isChecked` | boolean | — | — | **Checked** (VARIANT): `Off`, `On` |
| `isDisabled` | boolean | — | — | **State** (VARIANT): `Default`, `Disabled`, `Focus`, `Hover` |
| `label` | string | — | `'Toggle'` | — |
| `name` | string | — | — | — |

#### `fieldId`

Field ID
- Links the label to the toggle
- By default it is autogenerated by nanoid

#### `isChecked`

Checked attribute
- If true, sets the treatment to represent an on state
- If false, sets the treatment to represent an off state

Rendered with a PROPERTY binding (`.checked`), not `?checked`, matching
checkbox.ts. The content attribute only seeds the initial state: a user
click sets the input's dirty-checkedness flag, after which the attribute
no longer drives `.checked`. Bound as an attribute, setting `isChecked`
from code updated the host class and left the native input alone, so form
resets and state-driven toggles silently stopped working after one click.

#### `isDisabled`

Disabled attribute
- Changes the component's treatment to represent a disabled state

#### `label`

Label
- The text inside the label tag
- Does not display on the front-end, but is used for A11y

#### `name`

Name attribute
- The name attribute used on the toggle

## Variant axes

### `isChecked` (Figma property "Checked")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `Off`, `On`

### `isDisabled` (Figma property "State")

- Code values: —
- Figma options (unmapped 1:1 by design — labels differ on purpose, see `.altitude/contracts/README.md` § Deviations): `Default`, `Disabled`, `Focus`, `Hover`

## States

`focus`, `disabled`

## Slots (0)

_No slots declared._

## Events (1)

| Name | Description |
| --- | --- |
| `onToggleChange` | Fired when the toggle's checked state changes. Detail: `{ checked }`. |

## Accessibility

- ARIA-bearing attributes: —
- CSS parts: —

## Anatomy & token bindings

**Anatomy case measured:** `Checked=Off` (source: `measured`)

### Root — `<div class="al-c-toggle">`

_None._

### State overrides (measured, root node)

**`focus`**

| CSS property | Code token | Figma variable |
| --- | --- | --- |
| outline | `--al-theme-border-width-md` | `theme/border/width/md` |
| outline-color | `--al-theme-color-focus-ring` | `theme/color/focus-ring` |
| outline-offset | `--al-theme-border-width-md` | `theme/border/width/md` |
| outline-width | `--al-theme-border-width-md` | `theme/border/width/md` |

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/toggle/toggle.ts`
- Tag: `al-toggle`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (5)

`--al-theme-border-width-md`, `--al-theme-color-background-default-strong`, `--al-theme-color-focus-ring`, `--al-toggle-height`, `--al-toggle-width`

---

Source contract: `.altitude/contracts/altitude/al-toggle.contract.json`.
