<!-- GENERATED by scripts/contracts/build-component-docs.mjs from al-date-picker.contract.json + the altitude parity manifest — do not hand-edit; run `pnpm contracts:docs` -->

# al-date-picker — Date Picker

**Status:** `source` · **Version:** `1.0.0` · **Element:** `<—>`

Component: al-date-picker

## Figma

No Figma component set is mapped for `al-date-picker` in the **Altitude Design System** parity manifest (parity status: `missing-in-figma`). Map one in Figma, then run `pnpm run parity:seed`.

## Props (25)

| Name | Type | Values | Default | Figma |
| --- | --- | --- | --- | --- |
| `ariaDescribedBy` | string | — | — | — |
| `dateFormat` | enum | `MM/dd/yyyy`, `MMM dd yyyy`, `MMM dd, yyyy`, `dd MMM yyyy`, `dd/MM/yyyy`, `yyyy/MM/dd` | `'MMM dd, yyyy'` | — |
| `disabledMaxDate` | string | `any` | — | — |
| `disabledMinDate` | string | `any` | — | — |
| `errorNote` | string | — | — | — |
| `fieldId` | string | — | — | — |
| `fieldNote` | string | — | — | — |
| `hideLabel` | boolean | — | — | — |
| `isActive` | boolean | — | — | — |
| `isDayShortHand` | boolean | — | — | — |
| `isDisabled` | boolean | — | — | — |
| `isError` | boolean | — | — | — |
| `isoFormat` | string | — | `'yyyy-MM-dd'` | — |
| `isOptional` | boolean | — | — | — |
| `isRequired` | boolean | — | — | — |
| `label` | string | — | `'Date and Time'` | — |
| `multiYear` | number | — | `3` | — |
| `name` | string | — | — | — |
| `nextButtonText` | string | — | `'Next Month'` | — |
| `placeholder` | string | — | — | — |
| `previousButtonText` | string | — | `'Previous Month'` | — |
| `selectedDate` | string | `any` | — | — |
| `setActiveDate` | string | `any` | — | — |
| `startOnMonday` | boolean | — | — | — |
| `value` | string | — | — | — |

#### `ariaDescribedBy`

Aria describedby
- Used to connect the field note in the field to the input for accessibility

#### `dateFormat`

Specify date format for UI display

#### `disabledMaxDate`

Maximum date for disabled dates (format: yyyy/mm/dd)

#### `disabledMinDate`

Minimum date for disabled dates (format: yyyy/mm/dd)

#### `errorNote`

Error note displayed beneath the field when in an error state

#### `fieldId`

The unique id of the field

#### `fieldNote`

The field note displayed beneath the field

#### `hideLabel`

Hide label?

#### `isActive`

Indicates whether a date has been selected

#### `isDayShortHand`

Show the day of the week as a short hand, e.g. "M" for Monday

#### `isDisabled`

The field's disabled attribute

#### `isError`

Error state indicating an issue with the field

#### `isoFormat`

ISO Format for date

#### `isOptional`

Optional state
- Specifies that a field is optional and adds the text 'optional' to the label

#### `isRequired`

The field's required attribute

#### `label`

The field's label

#### `multiYear`

Amount of years to go before and after the current date

#### `name`

The field's name attribute

#### `nextButtonText`

Next month button text

#### `placeholder`

Placeholder attribute
- Specifies a short hint that describes the expected value of an input element

#### `previousButtonText`

Previous month button text

#### `selectedDate`

Holds the selected date value

#### `setActiveDate`

Set a specific date to be active

#### `startOnMonday`

Start the day of the week on Monday

#### `value`

The field's value attribute

## Variant axes

This component has no Figma `VARIANT`-bound prop — no variant axis to document.

## States

_No interaction states recorded._

## Slots (2)

| Slot | Description | Figma placeholder | Figma fan-out |
| --- | --- | --- | --- |
| `error` | If content is slotted, it will display in place of the errorNote property | — | — |
| `field-note` | If content is slotted, it will display in place of the fieldNote property | — | — |

## Events (3)

| Name | Description |
| --- | --- |
| `onDatePickerChange` | Fired when the selected date changes. Detail: `{ value }` — the formatted date string. |
| `onDatePickerClose` | Fired when the calendar overlay closes. Detail: `{ activeCalendar }` — the new open state. |
| `onDatePickerOpen` | Fired when the calendar overlay opens. Detail: `{ activeCalendar }` — the new open state. |

## Accessibility

- ARIA-bearing attributes: `ariaDescribedBy`
- CSS parts: —

## Anatomy & token bindings

Anatomy was not measured when this contract was authored (`anatomySource: "unavailable"`) — see `.altitude/contracts/README.md` § Anatomy availability is best-effort. No root-level token table to show.

## Conditional token bindings (T18 — derived from this component's own `.scss`)

This component's `.scss` has no BEM modifier classes and no nested pseudo-class/attribute state rules that resolve to a single `--al-*` token — no conditional bindings to derive (T18; see `.altitude/contracts/README.md`).

## Code

- Import: `@southleft/al-web-components/components/date-picker/date-picker.ts`
- Tag: `al-date-picker`
- Workspace: `@southleft/al-web-components`

## Tokens referenced (0)

_None._

---

Source contract: `.altitude/contracts/altitude/al-date-picker.contract.json`.
